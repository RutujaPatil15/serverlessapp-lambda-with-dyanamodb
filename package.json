{
  "name": "serverlessapp-lambda-with-dyanamodb",
  "version": "1.0.0",
  "description": "CRUD (Create, Read, Update, Delete) operations of DynamoDB, invoke AWS Lambda using serverless framework",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-offline": "^14.4.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.758.0",
    "@aws-sdk/client-lambda": "^3.758.0"
  }
}
